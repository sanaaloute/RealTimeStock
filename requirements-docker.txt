# Pinned for reproducible deploys. Versions validated end-to-end (see tests/).
tavily-python==0.7.26
python-dotenv==1.2.2
beautifulsoup4==4.15.0
requests==2.34.2
# playwright is preinstalled in the Docker base image
openpyxl==3.1.5
# Agent stack: LangChain, LangGraph, LLM providers
langchain-core==1.4.9
langchain-ollama==1.1.0
langchain-groq==1.1.3
langchain-openai==1.3.5
langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0
# PostgreSQL backend (used only when DATABASE_URL is set)
psycopg[binary,pool]==3.3.4
langgraph-checkpoint-postgres==3.1.0
pydantic==2.13.4
# Charts
matplotlib==3.11.0
numpy==2.4.2

# Chat API (bot talks to API, not agents directly)
fastapi==0.139.0
uvicorn[standard]==0.51.0
httpx==0.28.1
certifi==2026.2.25

# Telegram bot (job-queue for price target alerts)
python-telegram-bot[job-queue]==22.8
# Voice to text: local whisper (primary); Google Speech fallback needs ffmpeg for ogg/mp3
faster-whisper==1.2.1
SpeechRecognition==3.17.0
pydub==0.25.1
