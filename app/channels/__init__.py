"""Messaging channels.

Each channel adapts a messaging platform to the shared AI backend
(app.services.chat_service). The Telegram channel lives in `app/bot/`
(unchanged); `app/channels/whatsapp/` is the Evolution API channel.
"""
